name: Test GitHub Actions

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Confirm GitHub Actions
        run: echo "GitHub Actions is working successfully"
